This folder is intentionally minimal.

Per project constraints:
- SQLite DB must be stored in app root as `data.db` (for PyInstaller EXE stability).
- This folder can be used for future schema notes/backups if desired.