// Minimal helpers; charts initialized in dashboard template.

(function () {
  // Auto-focus on first input in auth forms
  const el = document.querySelector("input[autofocus]");
  if (el) el.focus();
})();